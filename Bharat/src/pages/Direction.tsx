import { Navbar } from "../components/Navbar";

export function Direction() {
    return (
        <div className="bg-black min-h-screen">
            <Navbar />
            {/* Content intentionally empty for now */}
        </div>
    );
}
